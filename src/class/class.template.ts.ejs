import { async } from '@angular/core/testing';
import { Observable, of as observableOf, throwError } from 'rxjs';
import { DocumentSearchBar, DocumentSearchBarModule } from '../_Shared/Scripts/ng-directives/DocumentSearchBar/directive.DocumentSearchBar';

<%- importMocks.join('\n') -%>

describe('<%- className %>', () => {
  let obj;

  <%- providerMocks.mocks.join('\n') %>

  beforeEach(() => {
    obj = new <%- className %>(<%- ctorParamJs %>);
  });

  <% for(var key in accessorTests) { %>
  <%- accessorTests[key] -%>
  <% } %>

  <% for(var key in functionTests) { -%>
  <%- functionTests[key] -%>
  <% } -%>

});
